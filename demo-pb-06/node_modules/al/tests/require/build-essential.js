/* Installs build tools for smartos*/
var pkg = require('../../lib/smartos').pkg,
    remoteFile = require('../../lib/smartos').remoteFile,
    bash = require('../../lib/smartos').bash;

module.exports.exec = function(opts) {
  describe('build-essential :', function() {
    it('should install gcc47-4.7', function(done) {
      pkg('gcc47-4.7', done);
    });
    it('should install gmake', function(done) {
      pkg('gmake', done);
    });
  });  
}


